<header class="s-header">

    <div class="header-logo">
        <a class="site-logo" href="index.html"><img src="https://i.hizliresim.com/3bszcbj.png" alt="Homepage"></a>
    </div>

    <nav class="header-nav-wrap">
        <ul class="header-nav">
            <li class="current"><a class="smoothscroll" href="#home" title="home">Anasayfa</a></li>
            <li><a class="smoothscroll" href="#about" title="about">Hakkımda</a></li>
            <li><a class="smoothscroll" href="#works" title="works">Projelerim</a></li>
            <li><a class="smoothscroll" href="#testimonials" title="blog">Referanslarım</a></li>
            <li><a class="smoothscroll" href="#contact" title="contact">İletişim</a></li>
        </ul>
    </nav>

    <a class="header-menu-toggle" href="#0"><span>Menü</span></a>

</header>
<script>
    // Aktif bölüme göre menüde .current sınıfı ekler
    window.addEventListener('scroll', function () {
        const sections = document.querySelectorAll("section[id], div[id]");
        const scrollY = window.pageYOffset;
        const navLinks = document.querySelectorAll('.header-nav li');

        sections.forEach(current => {
            const sectionHeight = current.offsetHeight;
            const sectionTop = current.offsetTop - 100;
            const sectionId = current.getAttribute("id");

            if (scrollY > sectionTop && scrollY <= sectionTop + sectionHeight) {
                navLinks.forEach(link => {
                    link.classList.remove("current");
                    if (link.querySelector("a").getAttribute("href") === "#" + sectionId) {
                        link.classList.add("current");
                    }
                });
            }
        });
    });
</script>